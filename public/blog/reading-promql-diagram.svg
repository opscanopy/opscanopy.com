<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 660" role="img" aria-label="A PromQL query decomposed into metric name, label matcher, range selector, rate function and aggregation">
  <defs>
    <marker id="ah" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-end">
      <path d="M0 0 L10 5 L0 10 z" fill="#10b981"/>
    </marker>
    <marker id="ahr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-end">
      <path d="M0 0 L10 5 L0 10 z" fill="#ef4444"/>
    </marker>
    <marker id="ahg" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-end">
      <path d="M0 0 L10 5 L0 10 z" fill="#94a3b8"/>
    </marker>
  </defs>

  <rect width="1200" height="660" rx="20" fill="#ffffff" stroke="#e2e8f0" stroke-width="2"/>

  <!-- Title -->
  <text x="40" y="58" font-family="ui-sans-serif, system-ui" font-size="30" font-weight="700" fill="#0f172a">Anatomy of a PromQL query</text>

  <!-- ============ THE QUERY (monospace tokens, spaced for callouts) ============ -->
  <!-- sum by (job) ( rate ( http_requests_total { status=~"5.." } [5m] ) ) -->
  <text x="60" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#0f172a">sum by (job)</text>
  <text x="232" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#94a3b8">(</text>
  <text x="260" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#0f172a">rate</text>
  <text x="322" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#94a3b8">(</text>
  <text x="350" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#0f172a">http_requests_total</text>
  <text x="654" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#0f172a">{status=~"5.."}</text>
  <text x="876" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#0f172a">[5m]</text>
  <text x="946" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#94a3b8">)</text>
  <text x="974" y="148" font-family="ui-monospace, monospace" font-size="26" fill="#94a3b8">)</text>

  <!-- ============ UNDERLINE BRACKETS under each token group ============ -->
  <!-- metric name underline -->
  <path d="M350 162 L350 174 L626 174 L626 162" fill="none" stroke="#10b981" stroke-width="2"/>
  <!-- label matcher underline -->
  <path d="M654 162 L654 174 L850 174 L850 162" fill="none" stroke="#10b981" stroke-width="2"/>
  <!-- range selector underline -->
  <path d="M876 162 L876 174 L930 174 L930 162" fill="none" stroke="#10b981" stroke-width="2"/>
  <!-- rate function brace -->
  <path d="M260 92 L260 80 L350 80 L350 92" fill="none" stroke="#10b981" stroke-width="2"/>
  <!-- aggregation brace -->
  <path d="M60 92 L60 80 L232 80 L232 92" fill="none" stroke="#10b981" stroke-width="2"/>

  <!-- ============ ROW 1: read inside-out — layers 1, 2, 3 ============ -->

  <!-- Layer 1: METRIC NAME -->
  <rect x="40" y="220" width="270" height="170" rx="12" fill="#f8fafc" stroke="#cbd5e1" stroke-width="2"/>
  <text x="60" y="252" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">1. Metric name</text>
  <text x="60" y="290" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">http_requests_total</text>
  <text x="60" y="328" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">The counter series you</text>
  <text x="60" y="352" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">start from. On its own</text>
  <text x="60" y="376" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">it is an instant vector.</text>

  <!-- Layer 2: LABEL MATCHER -->
  <rect x="340" y="220" width="270" height="170" rx="12" fill="#f8fafc" stroke="#cbd5e1" stroke-width="2"/>
  <text x="360" y="252" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">2. Label matcher</text>
  <text x="360" y="290" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">{status=~"5.."}</text>
  <text x="360" y="328" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">Regex match (=~) keeps</text>
  <text x="360" y="352" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">only 5xx series. Also</text>
  <text x="360" y="376" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">= != =~ !~</text>

  <!-- Layer 3: RANGE SELECTOR -->
  <rect x="640" y="220" width="270" height="170" rx="12" fill="#f8fafc" stroke="#cbd5e1" stroke-width="2"/>
  <text x="660" y="252" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">3. Range selector</text>
  <text x="660" y="290" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">[5m]</text>
  <text x="660" y="328" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">Turns it into a range</text>
  <text x="660" y="352" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">vector: every sample in</text>
  <text x="660" y="376" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">the 5-minute window.</text>

  <!-- Arrows chaining layers 1 -> 2 -> 3 -->
  <line x1="314" y1="305" x2="336" y2="305" stroke="#10b981" stroke-width="3" marker-end="url(#ah)"/>
  <line x1="614" y1="305" x2="636" y2="305" stroke="#10b981" stroke-width="3" marker-end="url(#ah)"/>

  <!-- Arrow down from range selector into rate function -->
  <line x1="775" y1="394" x2="775" y2="436" stroke="#10b981" stroke-width="3" marker-end="url(#ah)"/>

  <!-- ============ ROW 2: layer 4 (rate function) — emerald ============ -->
  <rect x="640" y="440" width="270" height="170" rx="12" fill="#ecfdf5" stroke="#10b981" stroke-width="2"/>
  <text x="660" y="472" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">4. rate() function</text>
  <text x="660" y="510" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">rate(...[5m])</text>
  <text x="660" y="548" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">Per-second average rate</text>
  <text x="660" y="572" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">of the counter; handles</text>
  <text x="660" y="596" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">counter resets for you.</text>

  <!-- Arrow left from rate function into aggregation -->
  <line x1="636" y1="525" x2="614" y2="525" stroke="#10b981" stroke-width="3" marker-end="url(#ah)"/>

  <!-- ============ ROW 2: layer 5 (aggregation) — emerald ============ -->
  <rect x="340" y="440" width="270" height="170" rx="12" fill="#ecfdf5" stroke="#10b981" stroke-width="2"/>
  <text x="360" y="472" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">5. Aggregation</text>
  <text x="360" y="510" font-family="ui-monospace, monospace" font-size="19" fill="#0f172a">sum by (job)</text>
  <text x="360" y="548" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">Collapses many series</text>
  <text x="360" y="572" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">into one rate per job;</text>
  <text x="360" y="596" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">keeps only the job label.</text>

  <!-- Arrow left from aggregation into result -->
  <line x1="336" y1="525" x2="314" y2="525" stroke="#10b981" stroke-width="3" marker-end="url(#ah)"/>

  <!-- ============ RESULT ============ -->
  <rect x="40" y="440" width="270" height="170" rx="12" fill="#f8fafc" stroke="#cbd5e1" stroke-width="2"/>
  <text x="60" y="472" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">Result</text>
  <text x="60" y="512" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">One 5xx error-rate</text>
  <text x="60" y="536" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">line per job, per</text>
  <text x="60" y="560" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">second, over the last</text>
  <text x="60" y="584" font-family="ui-sans-serif, system-ui" font-size="19" fill="#334155">5 minutes.</text>

  <!-- Trap callout (red): aggregate-before-rate -->
  <rect x="950" y="440" width="210" height="170" rx="12" fill="#fef2f2" stroke="#ef4444" stroke-width="2"/>
  <text x="970" y="472" font-family="ui-sans-serif, system-ui" font-size="21" font-weight="700" fill="#334155">Trap</text>
  <text x="970" y="510" font-family="ui-monospace, monospace" font-size="18" fill="#0f172a">rate(sum(...))</text>
  <text x="970" y="540" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">is a bug. Always</text>
  <text x="970" y="564" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">rate first, then</text>
  <text x="970" y="588" font-family="ui-monospace, monospace" font-size="18" fill="#0f172a">sum(rate(...))</text>

  <!-- Read direction note -->
  <text x="950" y="252" font-family="ui-sans-serif, system-ui" font-size="20" font-weight="700" fill="#334155">Read inside-out</text>
  <text x="950" y="284" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">Evaluate the</text>
  <text x="950" y="308" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">innermost</text>
  <text x="950" y="332" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">expression first,</text>
  <text x="950" y="356" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">then work</text>
  <text x="950" y="380" font-family="ui-sans-serif, system-ui" font-size="18" fill="#334155">outward.</text>
</svg>
