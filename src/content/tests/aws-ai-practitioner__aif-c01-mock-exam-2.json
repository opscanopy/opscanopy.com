{
  "category": "aws-ai-practitioner",
  "test": "aif-c01-mock-exam-2",
  "questions": [
    {
      "id": "q1",
      "prompt": [
        "A retail analytics team splits 240,000 historical orders into a training set, a validation set and a test set. Over three weeks the team compares eleven model variants and repeatedly checks each one against the validation set. A new manager asks why a third split is kept sealed and is only scored once, after the winning variant has been chosen.",
        "What is the purpose of the sealed test set in this workflow?"
      ],
      "options": [
        "It gives the model extra examples to learn from once the training accuracy stops improving",
        "It is where hyperparameters are chosen, because the validation set is reserved for early stopping",
        "It provides one unbiased estimate of performance on unseen data, because no modelling decision was made against it",
        "It replaces the validation set whenever the validation score turns out to be higher than the training score"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "A three-way split separates the data the model is fitted on, the data used to make choices about the model, and the data used to judge the result. Because the validation set was consulted eleven times, the score on it is optimistic — the choices have been fitted to it. The test set is scored once, after the final variant is fixed, so it estimates how the model will behave on data nobody has optimised against. Option A would let the model learn from the held-out rows, which destroys the measurement it exists to provide. Option B inverts the two roles: choosing hyperparameters is exactly what the validation set is for. Option D would pick whichever split flatters the model, which is not a measurement at all."
    },
    {
      "id": "q2",
      "prompt": [
        "A support organisation builds an Amazon Bedrock knowledge base over 4,000 troubleshooting articles, using a fixed chunk size of 200 tokens with no overlap. Reviewers find that answers stop mid-procedure, and that any step beginning near the end of a chunk loses the sentence that introduced it.",
        "Which change to the chunking configuration is most likely to improve answer quality?"
      ],
      "options": [
        "Reduce the chunk size to 50 tokens so that more chunks are retrieved for each question",
        "Stop chunking and embed each article as a single vector",
        "Increase the chunk size and add an overlap between consecutive chunks so that context spanning a boundary appears in both",
        "Switch the knowledge base to a larger foundation model for the generation step"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Chunk size and overlap decide how much usable context each retrieved unit carries. A 200-token chunk with no overlap severs procedures and strips the introductory sentence from any step that starts near a boundary, so the model is handed a fragment and answers from it. A larger chunk plus a modest overlap places boundary-spanning text in both neighbours, so whichever chunk is retrieved still reads coherently on its own. Option A shrinks the unit further and multiplies the fragmentation. Option B collapses a whole article into one averaged vector, which blurs many distinct topics together and retrieves the article for questions it barely covers. Option D changes the generator, not the broken fragments it is being given."
    },
    {
      "id": "q3",
      "prompt": [
        "A logistics organisation is preparing a responsible-AI review before it switches on an automated dispatch assistant that reassigns delivery routes across 14 depots. The reviewers' main objection is that once the assistant is live, nobody has a defined way to monitor its decisions, override a route it has changed, or shut it down if its behaviour drifts.",
        "Which AWS responsible AI dimension does that objection belong to?"
      ],
      "options": [
        "Veracity and robustness",
        "Transparency",
        "Controllability",
        "Governance"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "AWS groups responsible AI into named dimensions, and controllability is the one concerned with having mechanisms to monitor, guide and if necessary stop an AI system once it is running: override paths, escalation routes and an off switch. That is precisely what the reviewers say is missing. Option A, veracity and robustness, is about whether outputs are correct and stay reliable under unusual or adversarial conditions. Option B, transparency, is about making enough information available that stakeholders can judge a system and their interactions with it. Option D, governance, covers the policies, roles and oversight processes an organisation builds around its AI programme, not the runtime controls over one deployed system."
    },
    {
      "id": "q4",
      "prompt": [
        "A logistics company holds 90,000 photographs of damaged parcels in Amazon S3 and wants to train a model that classifies each photograph into one of six damage types. The photographs carry only a timestamp and a depot code. The operations team has no annotation tooling and no in-house labelling workforce.",
        "Which AWS service should the team use to produce the labelled dataset with the least operational effort?"
      ],
      "options": [
        "Amazon SageMaker Ground Truth, using its built-in image-classification labelling task and a managed workforce",
        "Amazon Rekognition Custom Labels, trained directly on the unlabelled photographs",
        "Amazon SageMaker Model Registry, to register each photograph against its damage type",
        "Amazon Comprehend custom classification, run over the photograph metadata"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Supervised image classification needs a label on every image, and 90,000 unlabelled photographs are the blocker. Amazon SageMaker Ground Truth supplies the labelling workflow, a built-in image-classification task template and access to a managed or vendor workforce, so the organisation neither builds annotation tooling nor recruits annotators. Option B still requires labelled examples before it can train a custom model, so it does not solve the problem it is offered for. Option C stores, versions and approves trained models; it has no labelling function and no concept of an image label. Option D classifies text, not images, and would itself need a labelled training sample before it could run."
    },
    {
      "id": "q5",
      "prompt": [
        "A SaaS provider serves 60 client organisations from a single Amazon Bedrock knowledge base built over documents all of them upload. Every document is tagged with its client identifier at ingestion. During a pilot, one client's assistant surfaced a passage from another client's file.",
        "Which approach prevents cross-client answers with the least redesign?"
      ],
      "options": [
        "Apply a metadata filter on the client identifier to every retrieval request so only that client's chunks are eligible",
        "Add a sentence to the prompt instructing the model to ignore documents belonging to other clients",
        "Reduce the number of retrieved passages from five to one",
        "Build a separate knowledge base and vector index for each of the 60 clients"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Isolation has to happen at retrieval, before any text reaches the model. Because each chunk already carries its client identifier as metadata, attaching a filter on that field to every query makes another tenant's chunks ineligible to be returned at all, which is a configuration change rather than a re-architecture. Option B relies on the model obeying an instruction about data it can already see, which is not an access control and fails the moment the model is persuaded otherwise. Option C only makes a leak less frequent, not impossible. Option D does isolate tenants, but it multiplies ingestion, syncing and cost by sixty and is far more redesign than the requirement calls for."
    },
    {
      "id": "q6",
      "prompt": [
        "A fintech organisation gives 60 developers access to Amazon Bedrock in a shared development account. Security has approved exactly two foundation models for internal use, and an audit found engineers experimenting with several others. The organisation wants access limited to the approved models without anyone reviewing usage by hand each month.",
        "Which approach enforces the restriction with least privilege?"
      ],
      "options": [
        "Attach an IAM policy that allows the bedrock:InvokeModel action only on the ARNs of the two approved models",
        "Attach an IAM policy that allows bedrock:* on all resources and review AWS CloudTrail monthly for use of unapproved models",
        "Apply a service control policy that denies every Amazon Bedrock action in the development account",
        "Configure an Amazon Bedrock guardrail that blocks prompts sent to models outside the approved list"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "IAM allows an action to be scoped to specific resources, and Amazon Bedrock models have ARNs, so a policy permitting bedrock:InvokeModel only on the two approved model ARNs makes a call to any other model fail at the API, with no human review in the loop. Option B grants everything and detects misuse after the fact; CloudTrail records the calls but does not prevent them. Option C blocks Amazon Bedrock outright, which stops the approved work as well. Option D misunderstands guardrails: a guardrail filters the content of prompts and responses for topics, words and sensitive data on the model it is applied to, and it has no say in which model a caller is permitted to invoke."
    },
    {
      "id": "q7",
      "prompt": [
        "A utility company is preparing a table to predict monthly customer churn. The raw columns hold a signup timestamp, a free-text tariff name and a raw meter reading in kilowatt-hours. A data scientist derives tenure in months from the timestamp, one-hot encodes the tariff name into eight columns and rescales the meter reading to a common range.",
        "Which stage of the machine learning lifecycle does this work belong to?"
      ],
      "options": [
        "Hyperparameter tuning",
        "Model evaluation",
        "Model monitoring",
        "Feature engineering"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Deriving tenure from a timestamp, one-hot encoding a categorical tariff name and rescaling a numeric reading are all transformations of raw columns into inputs an algorithm can learn from, which is the definition of feature engineering. It sits between data preparation and model training, and it is usually where the largest accuracy gains on tabular problems come from. Option A searches the settings that govern how training runs, and it happens only once the features already exist. Option B scores a trained model against held-out data and produces metrics, not columns. Option C watches an already deployed model for drift and degradation. None of those three changes the shape or encoding of the input data."
    },
    {
      "id": "q8",
      "prompt": [
        "An electronics distributor runs an assistant over 90,000 product pages using semantic search only. Plain-language questions work well, but a query for the part number XR-4410-B returns pages about similar parts and misses the exact one, because the identifier means little in embedding space.",
        "Which retrieval change addresses the failure?"
      ],
      "options": [
        "Raise the temperature of the generation model so it considers more possibilities",
        "Increase the number of retrieved chunks from 5 to 50",
        "Re-embed the catalogue with a model that has a larger context window",
        "Switch the knowledge base to hybrid search so exact keyword matching runs alongside semantic similarity"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Semantic search compares meaning, which is why it handles paraphrase well and handles rare literal strings such as part numbers, error codes and SKUs badly: two similar identifiers sit close together in embedding space. Hybrid search runs a keyword match in parallel with the vector match and merges the results, so an exact token like XR-4410-B is found on its literal form. Option A changes randomness in the output and cannot influence which documents were retrieved. Option B floods the prompt with 50 near-identical part pages and still may not include the exact one. Option C confuses the embedding model's input limit with its ability to match literal identifiers."
    },
    {
      "id": "q9",
      "prompt": [
        "A public-sector organisation is deciding whether to adopt a managed AWS AI service for face matching in a building-access system. Before it commits, the review board wants an AWS-published document setting out the service's intended use cases, its known limitations, the responsible-AI design and testing choices behind it, and best practices for deploying it well.",
        "Which resource provides that information?"
      ],
      "options": [
        "An Amazon SageMaker model card created for the service",
        "The AWS AI Service Card for that service",
        "The compliance reports available through AWS Artifact",
        "The service's API reference documentation"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "AWS AI Service Cards are AWS's own responsible-AI documentation for individual AI services and models: each card sets out intended use cases, design and testing choices, known limitations, and best practices for deployment and performance optimisation. That is exactly the material a review board needs before adopting a managed service. Option A inverts the relationship, because Amazon SageMaker model cards are where a customer documents a model it has built and trained itself, not something AWS publishes about its own services. Option C, AWS Artifact, is the portal for compliance reports and agreements such as SOC and ISO, which say nothing about a model's limitations. Option D describes parameters and syntax, not behaviour."
    },
    {
      "id": "q10",
      "prompt": [
        "A team is training a gradient-boosting model that predicts delivery time. Before training starts, someone must choose the learning rate, the number of trees and the maximum tree depth. When training finishes, the saved model contains the split thresholds the algorithm worked out from the data.",
        "Which statement correctly classifies these two groups of values?"
      ],
      "options": [
        "Both groups are parameters, because both end up stored inside the model file",
        "The learning rate, tree count and depth are hyperparameters set before training; the learned split thresholds are model parameters",
        "The learning rate, tree count and depth are model parameters; the split thresholds are hyperparameters produced by the tuning job",
        "Both groups are hyperparameters, because an automatic tuning job is able to search over either one"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "A hyperparameter is a value chosen before training that controls how training proceeds; learning rate, number of trees and maximum depth are the textbook examples, and an automatic tuning job searches over exactly these. A parameter is a value the algorithm learns from the data during training, such as the split threshold stored at each node, which no human sets directly. Option A uses storage location as the test, which is not what separates the two ideas. Option C reverses the terms outright. Option D is wrong because a tuning job can only search hyperparameters — learned thresholds are an output of training, never an input to it."
    },
    {
      "id": "q11",
      "prompt": [
        "A bank's retrieval-based assistant scores well on a generic summarisation benchmark, yet reviewers keep finding two distinct faults: answers that state figures the retrieved passages never contained, and answers that omit a figure the source document clearly provides. The team wants measures that separate the two.",
        "Which two evaluation measures should the team adopt?"
      ],
      "options": [
        "Faithfulness, which checks whether every claim in the answer is supported by the passages that were retrieved",
        "Perplexity of the foundation model measured on a general text corpus",
        "The average number of input tokens consumed per request",
        "Context recall, which checks whether retrieval actually returned the passages containing the information the answer needs",
        "BLEU measured against a set of reference translations"
      ],
      "correctAnswers": [
        0,
        3
      ],
      "explanation": "A retrieval pipeline has two failure points and they need separate measures. Faithfulness scores the generation step by asking whether each claim traces back to a retrieved passage, which is what catches invented figures. Context recall scores the retrieval step by asking whether the passages needed to answer were fetched at all, which is what catches the missing figure. Option B measures how well a model predicts general text and says nothing about either step. Option C is a cost and capacity figure, not a quality one. Option E is a translation metric based on n-gram overlap with references, and this task has no reference translations to compare against."
    },
    {
      "id": "q12",
      "prompt": [
        "A bank fine-tunes an Amazon Bedrock foundation model on 12 years of internal loan correspondence. Its key-management policy requires that the bank alone can control, rotate and revoke the key protecting the resulting custom model, and that revoking the key renders the model unusable.",
        "Which configuration satisfies that policy?"
      ],
      "options": [
        "Rely on the encryption Amazon Bedrock applies to custom models by default, which already protects them at rest",
        "Encrypt the training data in Amazon S3 with a customer managed key and leave the custom model on default encryption",
        "Block public access on the S3 bucket holding the training data and control access to the model with IAM policies alone",
        "Create a customer managed AWS KMS key, grant Amazon Bedrock use of it through the key policy, and encrypt the custom model with it"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Only a customer managed AWS KMS key gives the organisation ownership of the key policy, the rotation schedule and the ability to disable or schedule deletion of the key. Because the custom model is encrypted with that key, revoking it makes the model unusable, which is the control the policy demands. Option A is true as far as it goes, since Amazon Bedrock encrypts custom models by default, but a service-owned key cannot be controlled or revoked by the customer. Option B protects the training data while leaving the derived model, which carries the same information, outside the bank's key control. Option C confuses access control with encryption: bucket settings and IAM policies give nobody control of a key."
    },
    {
      "id": "q13",
      "prompt": [
        "A model that predicts warehouse pick times scores 61 percent accuracy on the training set and 60 percent on the test set. The team fitted a single linear model to three years of data whose demand pattern swings sharply with the season and with promotional weeks.",
        "What is happening, and what should the team try first?"
      ],
      "options": [
        "The model is overfitting; add regularisation and remove features until the gap closes",
        "The model has drifted; retrain it on the most recent month of data",
        "The model is underfitting; use a more expressive algorithm or add features that expose the seasonality",
        "The test split is leaking into training; reshuffle the split and retrain"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Training accuracy of 61 percent sits barely above test accuracy of 60 percent and both are poor, which is the signature of underfitting: the model is too simple to capture the pattern, so it performs badly everywhere rather than only on unseen data. A linear model against sharply seasonal demand is that situation exactly. The remedy is more capacity, or features such as month and promotion flags that make the pattern learnable. Option A treats the opposite condition, where a high training score sits far above a low test score, and regularisation would make this model simpler still. Option B assumes a deployed model whose incoming data has shifted, which is not in evidence. Option D would show as an unusually high test score, not a low one."
    },
    {
      "id": "q14",
      "prompt": [
        "A policy assistant retrieves 25 passages for every question and passes all of them into the prompt. Evaluation shows the genuinely correct passage is usually somewhere among the 25, but sits below weaker matches, and the model often answers from a passage that merely shares vocabulary with the question.",
        "Which change most directly improves the passages that reach the model?"
      ],
      "options": [
        "Increase the retrieved passage count from 25 to 100",
        "Add a re-ranking model that scores the 25 retrieved passages against the question and passes only the best few into the prompt",
        "Lower the generation temperature to zero",
        "Shorten the system prompt so more room is left for the passages"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Vector search is optimised for fast approximate recall across a large index, so it reliably gets the right passage into a candidate set but orders that set crudely. A re-ranking model reads each candidate together with the question and scores relevance directly, which is slower but far more accurate, and is applied only to the shortlist. Passing the top few reranked passages raises precision without losing recall. Option A retrieves four times as much noise and worsens the same problem. Option C makes the wording more deterministic but does not change which passages were supplied. Option D frees tokens without improving the relevance or ordering of what fills them."
    },
    {
      "id": "q15",
      "prompt": [
        "An insurer runs an assistant that answers broker questions about a 40-page policy-eligibility rulebook. Testing shows it sometimes states an eligibility conclusion that reads well but contradicts the rules. The compliance team wants each response checked against the rules themselves and a verifiable finding returned, rather than a confidence score.",
        "Which Amazon Bedrock Guardrails capability fits that requirement?"
      ],
      "options": [
        "Automated Reasoning checks, built from a formal model of the eligibility rules, which verify whether a response is logically consistent with them",
        "A contextual grounding filter set to its strictest threshold",
        "A denied topics policy listing every scenario in which a broker is ineligible",
        "A content filter for misconduct set to high strength"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Automated Reasoning checks are the Amazon Bedrock Guardrails policy that applies formal logic rather than probability: the team encodes the rulebook as variables and rules, and each response is checked against that model, returning a finding about whether the statement is valid, invalid, or outside the encoded scope. Option B, contextual grounding, tests whether a response is grounded in the retrieved passage and relevant to the query; useful, but it scores a response rather than proving consistency with a rule. Option C requires the team to enumerate every ineligible case by hand and blocks topics instead of checking reasoning. Option D filters harmful content, which is not the failure described."
    },
    {
      "id": "q16",
      "prompt": [
        "A payments team evaluates a model that flags suspicious card transactions. Only 0.8 percent of transactions are fraudulent, and the current model reaches 99.2 percent accuracy largely by predicting 'not fraud' almost every time. Leadership wants one headline number that moves when fraud is missed and also when genuine customers are wrongly blocked.",
        "Which evaluation metric should the team report?"
      ],
      "options": [
        "Accuracy, since it is already being measured and is comfortably high",
        "Mean squared error between the predicted and actual fraud flags",
        "The final training loss recorded at the end of the last epoch",
        "The F1 score, the harmonic mean of precision and recall"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "With 0.8 percent positives, always predicting the majority class scores above 99 percent accuracy while catching almost no fraud, so accuracy is worthless on this problem. The F1 score is the harmonic mean of precision and recall, so it falls when missed fraud rises and also when false alarms rise, which is the single balanced figure leadership asked for. Option A is the trap the scenario spells out. Option B is a regression metric for continuous targets and is not defined for a class label in any useful way. Option C measures how well optimisation converged on the training data, which says nothing about behaviour on unseen transactions or about the minority class."
    },
    {
      "id": "q17",
      "prompt": [
        "A travel company's support assistant runs on a prompt that developers paste and edit by hand for each new use. Tone drifts between teams, the model sometimes replies as a generic assistant rather than the company's support desk, and every request still needs the customer's booking reference and loyalty tier inserted.",
        "Which prompt-engineering practice addresses both problems?"
      ],
      "options": [
        "Raise the temperature so that replies sound more natural and less repetitive",
        "Ask the model to write its own system prompt at the start of each conversation",
        "Use one reusable prompt template that assigns the model an explicit role and takes the booking reference and loyalty tier as variables",
        "Fine-tune a separate model for each support team on that team's past replies"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Two techniques solve the two complaints at once. Role prompting states plainly who the model is acting as, which anchors register and scope instead of leaving the model to default to a generic assistant. A template with named variables keeps the surrounding wording identical for everyone while letting per-request values be substituted, so tone stops drifting with whoever last edited the text. Option A increases randomness, which widens the tone variation the team is trying to remove. Option B hands the one stable part of the system to the model to reinvent each session. Option D is an expensive customisation that entrenches the inconsistency between teams rather than resolving it."
    },
    {
      "id": "q18",
      "prompt": [
        "A healthcare provider in Germany is building a clinical-summary assistant. Its regulator requires that patient records, and the processing of any prompt containing them, remain inside the EU. The engineering team is choosing where to store the records and where to call the model.",
        "Which approach meets the residency requirement?"
      ],
      "options": [
        "Call the model in whichever Region has the lowest latency and encrypt the records with AWS KMS, since encryption satisfies residency",
        "Enable a cross-Region inference profile that may route requests to any commercial Region so capacity is never constrained",
        "Store the records in an EU Region and invoke the model in that same EU Region, choosing a model that is available there",
        "Store the records in an EU Region but invoke the model in us-east-1, where new models become available first"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Amazon Bedrock processes a request in the Region in which it is called, so residency is achieved by keeping both the data store and the model invocation in an EU Region, which makes model availability by Region part of the design decision. Option A confuses confidentiality with location: encryption protects the data but does not change which country processes it. Option B is the opposite of what a residency rule allows, because a cross-Region inference profile deliberately routes requests to other Regions, so an EU workload needs a profile scoped to the EU geography or none at all. Option D leaves the records in the EU but sends every prompt containing them abroad for processing."
    },
    {
      "id": "q19",
      "prompt": [
        "An energy retailer builds a model that predicts next-day electricity demand in megawatt-hours. Operations staff will read the error figure in a daily briefing and need it expressed in the same unit as the forecast itself, so that a reported error can be compared directly against the day's demand.",
        "Which evaluation metric should the team report?"
      ],
      "options": [
        "Mean absolute error, the average size of the misses in megawatt-hours",
        "Mean squared error, which is expressed in squared megawatt-hours and is dominated by a few large misses",
        "Area under the ROC curve",
        "F1 score"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "The deciding requirement is a metric expressed in the target's own unit. Mean absolute error averages the absolute difference between predicted and actual demand, so it is reported in megawatt-hours and reads directly as a typical miss that operations staff can compare against the day's total. Option B squares each error first, so the result is in squared megawatt-hours and cannot be read as a demand figure; it also weights a handful of large misses far more heavily than the brief asked for. Options C and D are classification metrics: area under the ROC curve summarises class separation across thresholds, and F1 balances precision and recall. Neither is defined for a continuous numeric prediction."
    },
    {
      "id": "q20",
      "prompt": [
        "A startup indexes 12 million document chunks for retrieval. Its embedding model can emit 1,024, 512 or 256 dimensions per vector. The vector database bill and query latency are both climbing, and the team has confirmed it can tolerate a small reduction in retrieval accuracy.",
        "What is the effect of selecting the smaller output dimension?"
      ],
      "options": [
        "Each vector stores fewer numbers, so index size and search cost fall, while some retrieval accuracy is typically given up",
        "The foundation model's context window shrinks in proportion to the vector size",
        "Retrieval accuracy improves, because shorter vectors carry less noise",
        "Existing vectors are converted automatically, so nothing has to be re-indexed"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "An embedding's dimensionality is simply how many numbers represent each chunk. Fewer dimensions means a smaller index, cheaper storage and faster similarity search, at the cost of a coarser representation that can blur fine distinctions between passages, so this is a deliberate cost-versus-recall trade-off rather than a free optimisation. Option B confuses the embedding vector's width with the generation model's context window; the two are unrelated. Option C states the trade-off backwards, since compression discards information rather than noise. Option D is the trap worth remembering: changing the embedding model or its dimension means the whole corpus must be re-embedded, because vectors of different shapes are not comparable."
    },
    {
      "id": "q21",
      "prompt": [
        "A marketing organisation generates several thousand product images a month with an image model on Amazon Bedrock. Months later, partners resize, crop and repost them, and the brand team needs a way to establish whether a given image now in circulation was produced by the model.",
        "Which approach supports that check most reliably?"
      ],
      "options": [
        "Overlay a visible logo in the corner of every generated image before publishing",
        "Rely on the invisible watermark the image model embeds in every image it produces, and use Amazon Bedrock's watermark detection to test an image later",
        "Record the prompt, model and timestamp of every generation request in a database",
        "Tag the objects in Amazon S3 with metadata marking them as AI-generated"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "The image model embeds an invisible, tamper-resistant watermark in every image it creates, and Amazon Bedrock offers watermark detection that reports whether an image carries it. Because the mark is designed to survive ordinary alterations such as resizing and cropping, it answers the question for an image already in circulation. Option A is defeated by a crop and degrades the creative work. Option C is a useful provenance record, but it can only be searched by request, not by presenting an arbitrary image found on a partner's site. Option D relies on S3 object metadata that exists only inside the bucket and is lost the moment the file is downloaded and reposted."
    },
    {
      "id": "q22",
      "prompt": [
        "A robotics team wants a warehouse robot to learn an efficient route policy across a 40-aisle floor. No dataset of correct routes exists and none can be produced. A simulator instead scores every attempt: a positive score for reaching the destination quickly, a negative score for each collision with a rack or a person.",
        "Which machine learning paradigm fits this problem?"
      ],
      "options": [
        "Supervised learning",
        "Reinforcement learning",
        "Unsupervised learning",
        "Semi-supervised learning"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Reinforcement learning is the paradigm in which an agent takes actions in an environment and improves a policy from reward and penalty signals rather than from labelled examples. A simulator that rewards speed and penalises collisions is precisely a reward function, and the absence of any dataset of correct routes rules out the label-driven paradigms. Option A needs input and output pairs with known correct answers, which the team explicitly cannot produce. Option C finds structure such as clusters in unlabelled data and has no notion of a good or bad action. Option D still depends on a labelled subset to anchor the unlabelled majority, and there is no labelled subset here."
    },
    {
      "id": "q23",
      "prompt": [
        "An insurer is starting a question-answering assistant over roughly 40,000 claims-handling documents held in an Amazon S3 bucket, answered by a foundation model on Amazon Bedrock. The two-person platform team has no capacity to build or operate a chunking, embedding and vector-store pipeline, and the account holds no existing search deployment to extend.",
        "Which service should the insurer use to supply the retrieved context with the least operational overhead?"
      ],
      "options": [
        "Amazon OpenSearch Service with an in-house ingestion job that chunks each document, calls an embedding model and writes the vectors",
        "Amazon Bedrock Knowledge Bases pointed at the Amazon S3 bucket, so that ingestion, chunking, embedding and retrieval are managed",
        "Amazon Kendra, relying on its managed index and connectors for the retrieval layer",
        "Amazon S3 Select queries issued against each document at question time"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Amazon Bedrock Knowledge Bases is the managed retrieval layer here: it reads the documents from the bucket, chunks them, calls an embedding model, maintains the vector store and returns the relevant passages to the model, so a small team gets retrieval-augmented generation without running a pipeline. Option A is that same pipeline built by hand, leaving chunking, embedding and relevance tuning for the team to operate. Option C was historically the enterprise search answer, but Amazon Kendra entered maintenance mode on 30 June 2026 and closed to new customers on 30 July 2026, so a build starting now cannot adopt it. Option D does no semantic matching: S3 Select filters rows inside one structured object and cannot rank prose by meaning."
    },
    {
      "id": "q24",
      "prompt": [
        "A pharmaceutical company trains a compound-screening model with Amazon SageMaker on proprietary assay data. Its security standard states that the training container must have no route to the internet for the duration of the job, while still reading its training data from Amazon S3.",
        "Which two configurations meet that standard?"
      ],
      "options": [
        "Run the job in a public subnet with a security group that permits outbound HTTPS only",
        "Enable network isolation on the training job so the training container has no outbound network access",
        "Enable Amazon SageMaker Debugger so the job is monitored for unexpected data transfers",
        "Turn on S3 Block Public Access for the bucket holding the training data and rely on that",
        "Launch the job into private VPC subnets with no NAT gateway and reach Amazon S3 through a VPC endpoint"
      ],
      "correctAnswers": [
        1,
        4
      ],
      "explanation": "Two complementary controls apply. Network isolation is the Amazon SageMaker setting that runs the training container with no outbound network access at all, so nothing inside the job can call out even if a dependency tries. Placing the job in private subnets with no NAT gateway removes the route to the internet at the network layer, and an Amazon S3 VPC endpoint keeps the data path on the AWS network. Option A still has a route to the internet, because restricting the protocol to HTTPS does not remove it. Option C is a monitoring feature for training metrics and tensors, not an egress control. Option D protects the bucket from anonymous readers but says nothing about what the container may connect to."
    },
    {
      "id": "q25",
      "prompt": [
        "A medical imaging startup holds only 4,000 labelled chest X-rays. A classifier trained from random initialisation performs barely above chance. A consultant suggests starting instead from a vision network already trained on millions of general photographs, then retraining only its final layers on the X-rays.",
        "Which technique is the consultant describing?"
      ],
      "options": [
        "Data augmentation",
        "Ensemble learning",
        "Unsupervised clustering",
        "Transfer learning"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Transfer learning reuses a model already trained on a large general dataset and adapts it to a smaller related task, typically by keeping the early layers that learned generic edges, textures and shapes and retraining only the final layers on the new labels. With 4,000 images that is far more effective than learning every weight from scratch. Option A manufactures extra training examples by rotating, cropping or adjusting the existing images; it helps with small datasets but does not describe starting from a pre-trained network. Option B trains several models independently and combines their votes, which does not address the shortage of labels. Option C groups unlabelled data and produces no classifier at all."
    },
    {
      "id": "q26",
      "prompt": [
        "A retailer's assistant runs on a large foundation model. Shoppers abandon the chat because nothing appears on screen for several seconds while the complete answer is generated. Internal evaluation shows most of these requests are short, well-defined classification replies that a smaller model answers correctly.",
        "Which two changes will most improve the experience?"
      ],
      "options": [
        "Increase the maximum output token limit so answers are never truncated",
        "Stream the response so that the first tokens render while the remainder is still being generated",
        "Route these short, well-defined requests to a smaller, faster model",
        "Raise the temperature so the model commits to an answer sooner",
        "Enlarge the model's context window"
      ],
      "correctAnswers": [
        1,
        2
      ],
      "explanation": "Perceived latency is dominated by time to first token, not by total generation time. Streaming lets the interface display words as they are produced, so the wait shrinks to a fraction of a second even when the full answer takes the same time overall. Choosing a smaller model for requests that evaluation shows it handles correctly cuts the real generation time as well, and lowers cost. Option A lengthens answers and therefore makes the wait worse. Option D controls randomness in token selection and has no bearing on speed. Option E governs how much text can be supplied in one request; it does not accelerate generation and a larger window often accompanies a slower, costlier model."
    },
    {
      "id": "q27",
      "prompt": [
        "An organisation is three weeks from launching a public-facing generative assistant. Its benchmark suite reports strong accuracy on 500 held-out questions, but the risk committee says it still has no evidence of how the assistant behaves when somebody actively tries to misuse it.",
        "Which pre-launch activity produces that evidence?"
      ],
      "options": [
        "Expand the accuracy benchmark from 500 to 5,000 held-out questions",
        "Run a load test at three times the expected peak request rate",
        "Have the development team re-read the system prompt and tighten its wording",
        "Run a red-teaming exercise in which a dedicated team deliberately attempts to elicit harmful, biased or policy-violating output, and feed the findings into guardrails before launch"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Red-teaming is the deliberate adversarial testing of a system before launch: a team acts as an attacker or bad-faith user, probing for harmful, biased, unsafe or policy-violating behaviour, and the findings feed the guardrails, the system prompt and the launch criteria. It is the only listed activity that produces evidence about misuse. Option A measures more of the same benign accuracy, which says nothing about adversarial behaviour. Option B measures capacity and latency under load, a reliability property. Option C is a reasonable habit, but it is a desk review by the people closest to the system, who are least likely to find the failure modes an outsider would deliberately hunt for."
    },
    {
      "id": "q28",
      "prompt": [
        "A streaming service wants a 'picked for this member' row on its home page that differs for every subscriber, drawn from each person's viewing history plus title metadata such as genre and release year. The company employs no ML engineers and wants a managed service that trains the model and serves recommendations in real time.",
        "Which AWS service should the company choose with the least development effort?"
      ],
      "options": [
        "Amazon Kendra",
        "Amazon Comprehend",
        "Amazon Personalize",
        "Amazon OpenSearch Service with keyword relevance ranking"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Amazon Personalize is the managed recommendation service: the company supplies an interactions dataset, a users dataset and an items dataset, and the service selects and trains a recipe and serves real-time recommendations through a campaign, with no model code written. Option A is an enterprise search service that answers a natural-language query over documents; it ranks documents against a query, not titles against a viewer's history. Option B extracts sentiment, entities and key phrases from text and builds no recommender. Option D would rank titles by keyword relevance, returning the same row to every subscriber, which is search rather than personalisation."
    },
    {
      "id": "q29",
      "prompt": [
        "A claims team builds a knowledge base over 30 years of claim files held as scanned PDFs, which are page images with no text layer. The ingestion job reports success, the vector index is almost empty, and every question comes back with no relevant results.",
        "What must be added to the ingestion pipeline?"
      ],
      "options": [
        "A larger embedding model, because the current one cannot encode PDF files",
        "A guardrail, because the scanned content is being blocked as sensitive information",
        "A larger chunk size, because each scanned page exceeds the current chunk limit",
        "Amazon Textract, to extract machine-readable text from the page images before chunking and embedding"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Embedding models consume text. A scanned PDF is a container of page images, so an ingestion job reads it, finds no extractable characters, and produces an index with nothing meaningful in it, which is exactly the symptom described. Textract performs optical character recognition and also recovers tables and form fields, so its output can then be chunked, embedded and retrieved normally. Option A misdiagnoses a missing-text problem as a model-capacity problem; no embedding model reads pixels as prose. Option B invents a filtering explanation that would show up as blocked responses, not an empty index. Option C tunes a parameter that only matters once there is text to divide in the first place."
    },
    {
      "id": "q30",
      "prompt": [
        "A retailer has enabled Amazon Bedrock model invocation logging, so prompts and responses are written to Amazon S3 and Amazon CloudWatch. A privacy review finds that shoppers sometimes paste card and account numbers into the chat box, that the logs are kept indefinitely, and that the whole analytics team can read them. The audit trail itself must stay in place.",
        "Which two measures address the privacy finding?"
      ],
      "options": [
        "Redact personal data from prompts and responses before they are written to the log destinations",
        "Disable model invocation logging so that no sensitive data is stored anywhere",
        "Set a CloudWatch Logs retention period and an S3 lifecycle rule so log data is deleted at the end of the window the privacy policy defines",
        "Transition the log objects to S3 Glacier Deep Archive so that keeping them indefinitely costs less",
        "Raise the strength of the guardrail's hate and violence content filters"
      ],
      "correctAnswers": [
        0,
        2
      ],
      "explanation": "The finding is that sensitive data is captured, then retained too long and read too widely. Redacting personal data before it reaches the log destinations means card and account numbers are never stored, and a defined CloudWatch Logs retention period plus an S3 lifecycle rule bounds how long the remaining records live. Option B removes the exposure by destroying the audit trail the stem says must remain. Option D changes the storage class and the bill, not the retention period or who can read the data. Option E tightens filters on harmful content, which is unrelated to personal data sitting in logs. Restricting read access would help as well, but was not among the choices."
    },
    {
      "id": "q31",
      "prompt": [
        "An airline wants a bot on its website and its phone line that recognises requests such as 'change my seat' or 'what is my baggage allowance', collects the booking reference and travel date needed to act on them, handles both typed and spoken input, and then calls an AWS Lambda function to complete the change.",
        "Which AWS service is purpose-built for this?"
      ],
      "options": [
        "Amazon Lex",
        "Amazon Polly",
        "Amazon Transcribe",
        "Amazon Kendra"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Amazon Lex is the service for building conversational interfaces across voice and text: it models intents such as changing a seat, elicits the slots the intent requires such as a booking reference, manages the multi-turn dialogue, and invokes an AWS Lambda function for fulfilment, with speech recognition and natural-language understanding built in. Option B turns text into speech and has no understanding of what a caller wants. Option C turns speech into text, which is one input step but provides no intent recognition, slot filling or dialogue management. Option D searches enterprise documents and returns relevant passages; it cannot run a booking dialogue or call fulfilment code."
    },
    {
      "id": "q32",
      "prompt": [
        "A logistics company builds an Amazon Bedrock agent that must look up a shipment, reschedule a delivery and issue a credit by calling three existing internal APIs. The AWS Lambda functions all work when invoked directly, but the agent reliably looks up shipments and never invokes the reschedule operation.",
        "Which part of the agent's configuration governs whether that operation is invoked?"
      ],
      "options": [
        "The temperature setting on the agent's foundation model",
        "The guardrail attached to the agent",
        "The action group's API schema, whose operation and parameter descriptions tell the agent what each call does and when to use it",
        "The agent's session memory retention period"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "An agent chooses tools by reading them. Each action group is described by an OpenAPI schema, or an equivalent function definition, and the natural-language summaries of each operation and parameter are what the model reasons over when deciding which call fits the request. A vague, missing or misleading description for the reschedule operation means the agent never recognises it as the right tool, however healthy the underlying Lambda function is. Option A changes randomness in wording, not tool selection. Option B can block content but does not teach the agent what a tool is for. Option D controls how much conversational context carries between sessions, which is unrelated to whether an operation is discoverable."
    },
    {
      "id": "q33",
      "prompt": [
        "A multinational manufacturer already has an internal responsible-AI policy and now wants an independent auditor to certify the management system it uses to develop and operate AI, in the same way its quality and information-security management systems are certified.",
        "Which standard is designed for that purpose?"
      ],
      "options": [
        "ISO/IEC 42001, a certifiable management system standard for artificial intelligence",
        "The NIST AI Risk Management Framework, organised around the Govern, Map, Measure and Manage functions",
        "The EU AI Act, which sorts AI systems into risk tiers with obligations attached to each",
        "SOC 2, which reports on the controls a service organisation operates for its customers"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "ISO/IEC 42001 is the international management system standard for artificial intelligence, structured like ISO 9001 and ISO/IEC 27001, so an organisation can build a conforming AI management system and have an accredited body certify it. Option B is voluntary guidance: the NIST AI Risk Management Framework describes the Govern, Map, Measure and Manage functions and is valuable for structuring risk work, but no one certifies an organisation against it. Option C is legislation, so conformity is a legal obligation that varies by risk tier rather than a certificate an organisation elects to pursue. Option D attests to a service organisation's controls and is not specific to how AI is built or operated."
    },
    {
      "id": "q34",
      "prompt": [
        "A hotel group receives about 30,000 free-text guest reviews a month. The analytics team needs two things from every review: whether its overall tone is positive, negative, neutral or mixed, and a list of the property names, cities and dates mentioned in the text. The team has no labelled data and does not want to train a model.",
        "Which two Amazon Comprehend capabilities should the team use?"
      ],
      "options": [
        "Sentiment analysis",
        "Topic modelling across the review corpus",
        "Named entity recognition",
        "Custom classification trained on a labelled sample of reviews",
        "Personally identifiable information detection and redaction"
      ],
      "correctAnswers": [
        0,
        2
      ],
      "explanation": "Sentiment analysis returns positive, negative, neutral or mixed for a document, which answers the first requirement, and named entity recognition extracts spans such as organisations, locations and dates, which answers the second. Both are pre-trained Amazon Comprehend APIs, so neither needs labelled data or a training job. Option B discovers unlabelled themes across a corpus, which is useful exploration but answers neither question the team asked. Option D would require labelling a sample and training a custom model, which the scenario rules out, and would still produce no entities. Option E finds and masks personal data such as names and card numbers, a privacy control rather than an analytics output."
    },
    {
      "id": "q35",
      "prompt": [
        "An HR agent returns a plausible but incorrect annual leave balance for an employee. The team cannot tell whether the agent consulted the knowledge base, called the leave-balance action group with the wrong employee identifier, or simply produced a number of its own.",
        "What should the team use to establish what happened?"
      ],
      "options": [
        "Enable the agent's trace and inspect the recorded reasoning steps, action-group invocations, parameters and knowledge-base lookups for that session",
        "Lower the temperature to zero and re-run the same question",
        "Increase the number of passages retrieved per question",
        "Fine-tune the model on a set of correct leave-balance answers"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "An agent's output is the end of a chain of decisions, so debugging it requires visibility into the chain rather than the final sentence. Enabling the trace records the model's rationale at each step, which action group was selected, the exact parameters sent, what the tool returned, and any knowledge-base lookup, which distinguishes a bad parameter from a missing retrieval from a fabricated figure. Option B makes the output reproducible without revealing any intermediate step. Option C tunes retrieval breadth before anyone knows retrieval is involved. Option D attempts an expensive fix for an unidentified fault, and would teach the model to recite balances rather than to look them up correctly. Note that Amazon Bedrock Agents is now Agents Classic and closed to new customers from 30 July 2026, with Amazon Bedrock AgentCore as its successor; AgentCore provides persistent, end-to-end tracing, so the same step-by-step evidence is what diagnoses this fault there too."
    },
    {
      "id": "q36",
      "prompt": [
        "An internal helpdesk portal renders the assistant's answer directly into the page as HTML so that tables and links keep their formatting. During a security review, a tester adds a knowledge-base article containing a script tag, asks a question that retrieves it, and watches the script run in the reviewer's browser.",
        "Which risk is this, and which control addresses it?"
      ],
      "options": [
        "Prompt injection; extend the system prompt with an instruction never to emit HTML",
        "Model denial of service; apply rate limiting to the portal",
        "Insecure output handling; treat model output as untrusted and encode or sanitise it before rendering it in the browser",
        "Training data poisoning; retrain the model with the offending article removed"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "This is insecure output handling, a named entry in the OWASP Top 10 for LLM applications: the application trusted text produced by a model and injected it into the page, so any markup the model reproduced from a retrieved document executes. The control is the ordinary one for untrusted input, namely encoding or sanitising the output and rendering it through a safe formatter rather than raw HTML. Option A names the wrong layer, because a prompt instruction is advisory and cannot serve as a security boundary. Option B addresses availability, not script execution. Option D misdiagnoses retrieval as training: the article was fetched at query time and never entered the weights, so retraining changes nothing."
    },
    {
      "id": "q37",
      "prompt": [
        "A retail business analyst who does not write code wants a model that predicts whether each store will miss its weekly sales target. The history sits in a CSV in Amazon S3 with the outcome already recorded in one column. The analyst wants a visual path to a trained model, with candidate algorithms and settings explored automatically.",
        "Which two AWS capabilities support this approach?"
      ],
      "options": [
        "Amazon SageMaker Model Registry",
        "Amazon SageMaker Canvas",
        "Amazon SageMaker Ground Truth",
        "Amazon SageMaker Autopilot",
        "Amazon SageMaker Feature Store"
      ],
      "correctAnswers": [
        1,
        3
      ],
      "explanation": "Amazon SageMaker Canvas gives a business analyst a visual, no-code workflow: import the CSV from Amazon S3, pick the target column, build the model and inspect predictions without writing a line of code. Canvas builds on Amazon SageMaker Autopilot, which automatically explores candidate algorithms and hyperparameter settings and returns a ranked list of trained candidates, and which can also be driven on its own. Option A stores, versions and approves models on their way to deployment; it trains nothing. Option C creates labelled datasets, but this CSV already carries the outcome column. Option E curates and shares features across teams and presumes an existing engineering workflow."
    },
    {
      "id": "q38",
      "prompt": [
        "A bank's Amazon Bedrock agent performs several tool calls and knowledge-base lookups per request. A compliance reviewer requires that the same denied-topic and sensitive-information rules apply to every model interaction the agent makes, including intermediate steps, and keep applying as engineers add new action groups.",
        "Which approach meets the requirement with the least ongoing effort?"
      ],
      "options": [
        "Append the rules as additional sentences at the end of the agent's instruction text",
        "Associate an Amazon Bedrock guardrail with the agent, so the policy applies across the agent's interactions",
        "Call a guardrail separately from inside each Lambda function behind an action group",
        "Apply a guardrail only to the final response that is returned to the user"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "A guardrail is a policy object that can be attached to an agent rather than invoked per call, so denied topics, sensitive-information filters and word policies are enforced consistently as the agent reasons and responds, and a new action group inherits the policy without anyone remembering to wire it in. Option A relies on instructions the model may deprioritise under a long or adversarial conversation, and instructions are not an enforced control. Option C duplicates the same policy across every function, so it drifts the first time one is updated and is forgotten on the next function added. Option D leaves intermediate steps unchecked, which is precisely the gap the reviewer named. Note that Amazon Bedrock Agents is now Agents Classic and closed to new customers from 30 July 2026, with Amazon Bedrock AgentCore as its successor, where the same agent-level guardrail enforcement is configured through AgentCore gateway policies rather than an association on the agent."
    },
    {
      "id": "q39",
      "prompt": [
        "A bank runs two AI workflows. In the first, an assistant drafts account-closure letters and a compliance officer reads and approves every letter before it is sent. In the second, a model blocks suspicious card transactions in real time while analysts watch a dashboard of aggregate decisions and step in when the pattern of blocks looks wrong.",
        "Which terms describe the two oversight arrangements, in order?"
      ],
      "options": [
        "Human-on-the-loop, then human-in-the-loop",
        "Human-in-the-loop, then human-on-the-loop",
        "Human-in-the-loop in both workflows",
        "Fully autonomous, then human-in-the-loop"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Human-in-the-loop means a person reviews and authorises each individual output before it takes effect, which is what the compliance officer does for every closure letter. Human-on-the-loop means the system acts on its own while people supervise its behaviour in aggregate and intervene when something looks wrong, which is the fraud-blocking arrangement. Option A reverses the two. Option C ignores the fact that no analyst approves individual card blocks, since a real-time block cannot wait for a person, which is exactly why oversight moved to the aggregate view. Option D calls the fraud model fully autonomous, but supervision with the power to intervene is what distinguishes human-on-the-loop from autonomy."
    },
    {
      "id": "q40",
      "prompt": [
        "A team is estimating the monthly bill for a customer-support summariser on Amazon Bedrock. The pricing page quotes a price per 1,000 input units and a higher price per 1,000 output units, and the model card states the maximum request size in that same unit rather than in words, characters or megabytes.",
        "What is that unit, and what does it represent?"
      ],
      "options": [
        "A character, because text models bill per character of input and output",
        "A token, a chunk of text roughly the size of a short word or a word fragment, which is the unit the model processes",
        "An embedding, a numeric vector the model produces for each sentence of the request",
        "A parameter, one of the learned weights the model applies while answering the request"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Foundation models split text into tokens, sub-word chunks that average roughly three quarters of a word in English, and both pricing and context limits are expressed in tokens because that is the unit the model actually consumes and produces. Estimating the bill therefore means estimating input and output tokens, not words. Option A is wrong because billing is not per character; one long word and one short word can each be a single token. Option C describes a numeric vector used for semantic search, which is a separate model's output and never a billing unit for text generation. Option D describes a fixed learned weight inside the model, a property of the model rather than something a request consumes."
    },
    {
      "id": "q41",
      "prompt": [
        "A media organisation decides to fine-tune an Amazon Bedrock foundation model so it writes headlines in the publication's house style. It holds 40,000 published articles with their headlines in a content database, and has a small editorial team available to review candidate examples.",
        "Which two steps are required to prepare the training data?"
      ],
      "options": [
        "Upload the raw article PDFs to Amazon S3 without labels and let the service infer the pairs",
        "Set the temperature to zero on every training record",
        "Convert the data to JSONL, one record per line pairing the input prompt with the desired headline, and store it in Amazon S3",
        "Increase the model's context window before the training job is started",
        "Curate a smaller set of high-quality, representative examples and hold some back as a validation set"
      ],
      "correctAnswers": [
        2,
        4
      ],
      "explanation": "Fine-tuning is supervised, so it needs labelled pairs in the expected format: a JSONL file in Amazon S3 where each line carries the input and the target output the model should learn to produce. Quality then beats volume, because a curated, representative sample teaches the style cleanly while a dump of every article teaches the model the newsroom's inconsistencies, and a held-back validation set is what shows whether the job is learning or memorising. Option A describes unlabelled data, which suits continued pre-training, not fine-tuning. Option B applies an inference parameter to training records, where it has no meaning. Option D treats the context window as a tunable setting; it is a fixed property of the model."
    },
    {
      "id": "q42",
      "prompt": [
        "A media company wants to bring an open-weight model it found on a public model hub into Amazon Bedrock through Custom Model Import, for an internal summarisation tool. The security team asks how the organisation will manage the supply-chain risk that comes with weights it did not train itself.",
        "Which approach addresses that risk?"
      ],
      "options": [
        "Obtain the weights from the verified publisher, check the published checksums and the licence, scan the downloaded artefacts, and evaluate the model in an isolated account before any production use",
        "Rely on the shared responsibility model, since AWS validates the safety of any model imported into Amazon Bedrock",
        "Treat open weights as inherently safe, because a published architecture can be inspected by anyone",
        "Attach a guardrail to the imported model, which removes the need to establish where the weights came from"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Imported weights become part of the organisation's software supply chain, so the controls are the ones used for any third-party dependency: take the artefacts from the verified publisher, verify checksums, read the licence for the terms it places on commercial use, scan what was downloaded, and test behaviour in an isolated account before production. Option B misstates the shared responsibility model, since AWS operates the infrastructure while choosing and validating an imported model stays with the customer. Option C confuses an open architecture with vetted weights, because training data and learned behaviour are not visible by inspection. Option D filters content at run time and says nothing about provenance."
    },
    {
      "id": "q43",
      "prompt": [
        "A team lead is explaining to a non-technical steering group why current large language models cope with long, complex sentences far better than the sequential architectures they replaced. She wants to name the architecture and the mechanism that lets the model weigh every word in the input against every other word at once.",
        "Which architecture and mechanism is she describing?"
      ],
      "options": [
        "A convolutional network using pooling layers",
        "A decision-tree ensemble using feature importance scores",
        "A recurrent network using a fixed sliding window",
        "A transformer using self-attention"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "The transformer is the architecture behind current large language models, and self-attention is the mechanism that lets every position in the input attend to every other position and weigh their relevance, which is why relationships between distant words survive far better than in the sequential designs that came before. Option A describes convolutional networks, which excel at images and capture local patterns rather than arbitrary long-range word relationships. Option B describes a classical tabular technique with no notion of sequence at all. Option C describes the older recurrent approach the transformer displaced, which processes one step at a time and loses information across long spans."
    },
    {
      "id": "q44",
      "prompt": [
        "A team holds two candidate system prompts for its product-recommendation assistant. Offline scoring on 200 saved questions rates them within a point of one another, but the metric the business actually cares about is whether shoppers accept the recommendation they are shown.",
        "How should the team decide between the two prompts?"
      ],
      "options": [
        "Choose the shorter prompt, because it consumes fewer input tokens per request",
        "Ask the foundation model which of the two prompts it prefers",
        "Re-run the offline scoring at a higher temperature to break the tie",
        "Run an A/B test that sends a share of live traffic to each variant and compare acceptance rate over a fixed period"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "When two variants are indistinguishable offline and the deciding measure is a user behaviour, the only reliable evidence comes from real traffic. An A/B test splits users between the variants, holds everything else constant, and compares the business metric over a defined window, which is how a prompt change is tied to acceptance rate rather than to a proxy score. Option A optimises cost while ignoring the stated decision criterion, and cost is not what is being compared here. Option B asks the system under test to grade itself, which produces a preference with no relationship to shopper behaviour. Option C adds randomness to a scoring run, turning a tie into noise rather than into evidence."
    },
    {
      "id": "q45",
      "prompt": [
        "A jobs marketplace recommends listings and retrains weekly on the clicks its own recommendations produce. Over six months, listings from one metropolitan area come to dominate the recommendations, engagement data from smaller regions thins out, and those regions are then recommended even less at each retraining.",
        "Which two actions break the feedback loop?"
      ],
      "options": [
        "Reserve a share of impressions for exploration, so listings the current model ranks low still receive exposure and generate data",
        "Track the distribution of recommendations and engagement by region over time, so the drift is detected as it happens",
        "Retrain the model daily rather than weekly so it adapts to the newest click data faster",
        "Move to a larger model that can learn more nuanced regional preferences",
        "Drop the region field from the training features so the model cannot use it"
      ],
      "correctAnswers": [
        0,
        1
      ],
      "explanation": "The loop is self-reinforcing because the training data is generated by the model's own recommendations: regions shown less produce fewer clicks, which the next retraining reads as lower interest. Reserving impressions for exploration injects data the current ranking would never collect, and monitoring recommendation and engagement distributions by region turns a slow drift into something visible while it is still small. Option C makes the loop turn faster and amplifies the imbalance sooner. Option D changes model capacity, not the biased data it learns from. Option E removes one field while proxies such as postcode and employer remain, and it also destroys the organisation's ability to measure the imbalance."
    },
    {
      "id": "q46",
      "prompt": [
        "A marketing team uses a text-to-image model that begins each render from a field of pure random noise and then, over 40 steps, removes a little of the estimated noise at a time while the prompt steers each step, until a coherent picture emerges.",
        "Which family of model is being described?"
      ],
      "options": [
        "A diffusion model",
        "A large language model using next-token prediction",
        "An embedding model",
        "A classical linear regression model"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Diffusion models are trained to reverse a noising process, so generation starts from random noise and iteratively removes a little estimated noise at each step while the text prompt conditions every step, until an image remains. That step-by-step denoising is exactly the behaviour described, and the step count is the knob the team is setting at 40. Option B describes how a text model generates, predicting one token at a time from the tokens before it, which produces text rather than pixels. Option C turns input into a fixed numeric vector representing meaning and generates nothing at all. Option D fits a line through numeric data and has no generative capability."
    },
    {
      "id": "q47",
      "prompt": [
        "An insurer's assistant drafts settlement letters. Most drafts are sound, but any draft quoting a payout above 10,000 US dollars must be read by a licensed adjuster before it is sent, and the adjuster's decision has to be recorded so auditors can review it later.",
        "Which approach implements the review step?"
      ],
      "options": [
        "Use a human-in-the-loop review workflow, such as the pattern Amazon Augmented AI implements, triggered by the payout condition to route the draft to a licensed reviewer and store the outcome",
        "Add a guardrail that blocks any answer containing a figure above 10,000 US dollars",
        "Have the model rate its own confidence and send only the drafts it rates as low confidence",
        "Fine-tune the model on previously approved letters so that review becomes unnecessary"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "This is a human-in-the-loop requirement: a defined condition diverts an item to a qualified person, the person approves or amends it, and the decision is retained as evidence. A human review workflow such as Amazon Augmented AI provides exactly that structure, including the reviewer task and the stored result an auditor can inspect. Option B suppresses the draft rather than routing it, so a legitimate large settlement never reaches the adjuster at all and no decision is recorded. Option C substitutes a self-reported score for the stated monetary threshold, and models are poorly calibrated judges of their own output. Option D cannot satisfy a regulatory review obligation, because customisation is not accountability. Note that Amazon Augmented AI is no longer open to new customers, so a new build implements the same review-and-record pattern with AWS Step Functions and a task queue, or an equivalent workflow service."
    },
    {
      "id": "q48",
      "prompt": [
        "A SaaS vendor operates one Retrieval Augmented Generation assistant for 300 customer organisations, with every customer's documents held in a single vector index. A tester signed in as one customer asks a broad question and receives a passage drawn from another customer's board minutes.",
        "Which change enforces tenant isolation most reliably?"
      ],
      "options": [
        "Add an instruction to the system prompt telling the model to use only the signed-in customer's documents",
        "Enable personally identifiable information redaction on the assistant's responses",
        "Log every retrieval so that cross-tenant answers can be investigated after they occur",
        "Enforce the tenant identity in the retrieval layer itself, with a separate knowledge base per tenant or a mandatory filter the application derives from the authenticated session"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Tenant isolation has to be enforced where the data is fetched, not where the text is generated. Either give each tenant its own knowledge base, or apply a tenant filter that the application derives from the authenticated session and attaches to every retrieval, so nothing a user types can widen the scope. Option A relies on a system prompt, which is an instruction the model may ignore and an attacker may talk around, and is not an access-control boundary. Option B redacts personal data from responses but would happily return another tenant's strategy discussion. Option C is detective only: by the time anyone reads the log, the passage has already been disclosed."
    },
    {
      "id": "q49",
      "prompt": [
        "A team tuning a product-description generator holds temperature fixed and experiments with two other sampling settings. The first caps the candidate pool at a fixed count of the most likely next tokens. The second keeps the smallest set of next tokens whose probabilities add up to a chosen threshold, so the pool size varies with the model's confidence.",
        "Which two settings are these, in that order?"
      ],
      "options": [
        "Top-p, then top-k",
        "Stop sequences, then top-k",
        "Top-k, then top-p",
        "Maximum output tokens, then temperature"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Top-k and top-p both restrict which next tokens may be sampled, but by different rules. Top-k keeps a fixed number of the highest-probability candidates no matter how confident the model is, while top-p, also called nucleus sampling, keeps the smallest set whose cumulative probability reaches the threshold, so the pool narrows when the model is confident and widens when it is not. Option A names the right pair in the wrong order. Option B substitutes a stop sequence, which ends generation when a chosen string appears and has nothing to do with candidate selection. Option D names an output-length cap plus the randomness control the scenario explicitly held fixed."
    },
    {
      "id": "q50",
      "prompt": [
        "A team evaluates a summarisation model against human-written reference summaries. A generated summary saying the outage lasted two hours scores poorly against a reference saying the service was down for 120 minutes, even though every human reviewer marks the summary correct.",
        "Which metric addresses that limitation?"
      ],
      "options": [
        "BLEU, which measures n-gram precision against the reference text",
        "Perplexity, which measures how surprised the model is by the reference text",
        "BERTScore, which compares generated and reference text using contextual embeddings and so credits semantically equivalent wording",
        "An F1 score computed over the exact words the two texts share"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "The failure is a property of overlap-based scoring: two hours and 120 minutes share no words, so any metric counting matching n-grams penalises a correct paraphrase. BERTScore instead embeds both texts and compares them in vector space, so wording that means the same thing scores highly, which is why it is offered alongside overlap metrics for open-ended generation. Option A is the overlap family that caused the problem, and it is aimed at translation besides. Option B measures how well a model predicts text and produces no comparison between a candidate answer and a reference. Option D is another exact-token measure and would penalise the paraphrase for the same reason."
    },
    {
      "id": "q51",
      "prompt": [
        "A university's admissions team is defining fairness for a model that shortlists applicants. One group argues that the proportion of applicants shortlisted should be equal across demographic groups. Another argues that among applicants who would have gone on to graduate, the proportion shortlisted should be equal across those groups. Measured on the same model, the two disagree.",
        "Which statement describes the situation correctly?"
      ],
      "options": [
        "The two are equivalent, so a model that satisfies one automatically satisfies the other",
        "Only the second is a real fairness definition; the first is simply a quota",
        "They are two different fairness definitions, demographic parity and equal opportunity, which can conflict, so the organisation must choose the one its context calls for and document that choice",
        "The disagreement disappears once the demographic attribute is removed from the training data"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Fairness has several mathematical definitions that do not agree with one another. Demographic parity requires equal selection rates across groups; equal opportunity requires equal true positive rates, that is, equal selection among those who would have succeeded. Where base rates differ between groups, a model can satisfy one and fail the other, so the organisation must choose deliberately, justify the choice for its context and record it. Option A asserts an equivalence that holds only in special cases. Option B dismisses a definition that is standard and appropriate in some settings. Option D describes fairness through unawareness, which fails because correlated features act as proxies for the removed attribute."
    },
    {
      "id": "q52",
      "prompt": [
        "A support tool asks a model for a single reply to one customer message, but the model keeps running past the reply and inventing a further exchange that always begins with the line 'Customer:'. Cutting the text off afterwards in application code leaves half-written sentences in the transcript log.",
        "Which inference setting should the team configure so that generation ends cleanly at that boundary?"
      ],
      "options": [
        "Lower the temperature to zero",
        "Configure a stop sequence of 'Customer:'",
        "Reduce top-k to 1",
        "Reduce the maximum output tokens until the invented exchange no longer fits"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "A stop sequence tells the service to end generation the moment the model emits a chosen string, and the string itself is not returned, so the reply finishes at the boundary the team cares about instead of being trimmed afterwards. Setting it to 'Customer:' stops generation exactly where the invented exchange would begin. Option A makes wording more predictable but does nothing about a model that predictably continues the transcript. Option C narrows sampling to the single likeliest token, which again changes wording rather than where generation ends. Option D is the same truncation the team already has: a token cap cuts off wherever the budget runs out, which is precisely what leaves half-written sentences."
    },
    {
      "id": "q53",
      "prompt": [
        "A procurement committee shortlists a foundation model because it leads a public MMLU leaderboard. The model is intended to classify inbound maintenance tickets into 40 categories, written by technicians in site-specific shorthand that appears nowhere in any public dataset.",
        "How much weight should the MMLU result carry in this decision?"
      ],
      "options": [
        "It is decisive, because a higher MMLU score means higher accuracy on any task",
        "It is a useful general signal, but the model must still be evaluated on a labelled sample of the organisation's own tickets",
        "It should be ignored, because public benchmarks measure only response speed",
        "It can replace internal evaluation, provided the model is fine-tuned afterwards"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Public benchmarks such as MMLU measure broad multiple-choice knowledge across academic subjects. That is a reasonable way to shortlist candidates, but it is a proxy: it says nothing about 40 in-house categories expressed in shorthand the benchmark has never seen, and leaderboard results can be inflated by training data that overlaps the test set. The decision needs a labelled sample of real tickets scored for the accuracy the business will actually experience. Option A treats a general score as transferable to every task. Option C misstates what the benchmark measures, since MMLU scores knowledge rather than latency. Option D defers evaluation behind a customisation step that itself needs labelled data to be judged."
    },
    {
      "id": "q54",
      "prompt": [
        "A company runs 40 AWS accounts in which data science teams create Amazon SageMaker notebook instances and endpoints at will. Governance wants continuous detection of notebook instances that allow direct internet access and of endpoints that are not encrypted with a KMS key, with the results visible in one place.",
        "Which approach meets the requirement with the least ongoing effort?"
      ],
      "options": [
        "Schedule a quarterly manual review of the Amazon SageMaker console in each account",
        "Evaluate the SageMaker resources continuously with AWS Config rules and aggregate the findings across all accounts in AWS Security Hub",
        "Enable AWS CloudTrail everywhere and search the logs for CreateNotebookInstance calls",
        "Apply a service control policy denying the CreateNotebookInstance action in every account"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "AWS Config evaluates resource configurations continuously and ships managed rules for exactly these SageMaker properties, recording each resource as compliant or non-compliant as it changes, while AWS Security Hub aggregates those findings across accounts and Regions into a single view. Option A produces a snapshot every three months, so a misconfigured notebook can run for a quarter unnoticed. Option C records who called which API, which is valuable during an investigation but does not report the current state of a resource that was modified later. Option D prevents new notebook instances altogether, blocking legitimate work while doing nothing about the endpoints or the resources that already exist."
    },
    {
      "id": "q55",
      "prompt": [
        "A bank's assistant must always answer in formal British English, must never give investment advice, and must decline questions outside retail banking. Those instructions are identical on every one of the 40,000 daily requests, while the customer's question differs each time.",
        "How should the team structure each request to the model?"
      ],
      "options": [
        "Put the standing instructions in the system prompt and the customer's question in the user message",
        "Put the standing instructions in the user message, ahead of the customer's question, on every request",
        "Put both the standing instructions and the question in the system prompt and leave the user message empty",
        "Put the standing instructions in the model's first response so later turns inherit them"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "The system prompt carries standing instructions — role, tone, scope and refusal rules — that apply to every turn, while the user message carries the request that changes. Separating them keeps the policy in one reviewable place, lets the model treat it as higher-priority context than the customer's text, and makes it easy to version. Option B works mechanically but buries the policy inside user content, where it competes with whatever the customer types and is far harder to govern. Option C leaves no user turn for the model to answer. Option D misunderstands the roles: model output is a result, not a channel through which instructions are issued to later turns."
    },
    {
      "id": "q56",
      "prompt": [
        "A finance team uses a model for multi-step arithmetic reasoning over expense reports. Running at a non-zero temperature, the model occasionally reaches a wrong total by slipping on one intermediate step. The team can afford five model calls per report and wants the reported figure to be right more often.",
        "Which prompting technique addresses this?"
      ],
      "options": [
        "Raise the temperature so the model explores a wider range of reasoning paths",
        "Raise the maximum output token limit so the reasoning is never cut short",
        "Add a negative prompt listing the totals the model must avoid producing",
        "Sample several independent answers to the same prompt and take the most frequent result, a self-consistency approach"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Self-consistency runs the same prompt several times at a non-zero temperature, producing independent reasoning paths, and then takes the answer that appears most often. Arithmetic slips tend to differ from one another while the correct total recurs, so majority voting over five samples raises accuracy at the cost of extra calls, which the team has already budgeted for. Option A adds variation with no mechanism for choosing between the results, making the problem worse rather than better. Option B governs how long an answer may be, not whether the arithmetic in it is right. Option C borrows negative prompting from image generation and is meaningless here, since nobody knows in advance which wrong totals to exclude."
    },
    {
      "id": "q57",
      "prompt": [
        "A claims intake pipeline feeds a model's output straight into a downstream parser that must read the policy number, the incident date and the claim amount. The model answers in prose today, and the parser breaks whenever the model rewords a sentence or reorders the three values.",
        "Which prompting change should the team make?"
      ],
      "options": [
        "Lower the temperature so that the prose wording stays stable between requests",
        "Move to a model with a larger context window so the whole claim fits comfortably",
        "Instruct the model to return only JSON with a named set of keys, and include one example of the exact structure",
        "Add a stop sequence after the first sentence so the parser only ever sees one line"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "The downstream system needs a stable machine-readable contract, so the prompt should demand JSON only, name the exact keys expected and show one example of the structure; the parser then depends on a schema rather than on phrasing. Option A makes prose more repeatable but leaves the parser matching free text, which breaks again the moment the model rephrases. Option B addresses input capacity, which is not the failure here — the claim text already fits, and a bigger window does not make output parseable. Option D truncates output early and would cut the answer off mid-structure, losing two of the three values rather than making any of them reliable to read."
    },
    {
      "id": "q58",
      "prompt": [
        "An interactive chat assistant produces answers of about 600 tokens. Users complain that nothing at all appears for several seconds after they press send. Once the answer does appear, they judge it accurate, and the total generation time is within the team's agreed budget.",
        "Which change improves the perceived responsiveness without changing the model or the answer?"
      ],
      "options": [
        "Increase the maximum output tokens so the model is able to finish sooner",
        "Stream the response so tokens are displayed as they are generated, cutting the time to first token",
        "Switch the request path to batch inference",
        "Raise the temperature so the model begins generating earlier"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "Streaming returns tokens as the model produces them, so the first words reach the screen almost immediately and the reader watches the answer build. Total generation time is unchanged; what improves is the time to first token, which is precisely what the complaint is about. Option A raises the ceiling on answer length, which if anything lengthens generation rather than shortening the wait. Option C is designed for large asynchronous jobs with nobody waiting and would make an interactive chat dramatically worse. Option D confuses a sampling control with latency: temperature changes how each next token is chosen, not when generation starts."
    },
    {
      "id": "q59",
      "prompt": [
        "A publisher must classify 4 million archived articles by topic using a foundation model. The job is scheduled to run overnight, no user is waiting on any individual result, and the complete output is needed by nine the following morning. Cost per article is the deciding factor.",
        "Which Amazon Bedrock inference option should the team choose at the lowest cost?"
      ],
      "options": [
        "Batch inference, which processes a file of prompts from Amazon S3 asynchronously and is priced below on-demand for the same model",
        "On-demand inference, issuing one synchronous request per article",
        "Provisioned Throughput with a six-month commitment",
        "A streaming request per article, to reduce the time to first token"
      ],
      "correctAnswers": [
        0
      ],
      "explanation": "Amazon Bedrock batch inference reads a file of prompts from Amazon S3, processes them asynchronously and writes the results back to S3, and it is priced below on-demand for the same model — exactly the trade the publisher wants, since nothing is waiting on an individual article overnight. Option B pays full on-demand price four million times and adds the burden of orchestrating that many synchronous calls. Option C buys reserved capacity for sustained, predictable, latency-sensitive traffic and commits spend for months, far more than a one-off archival pass needs. Option D optimises perceived latency for an interactive reader, and there is no interactive reader in this workload."
    },
    {
      "id": "q60",
      "prompt": [
        "A team wants to assemble a multi-step generative workflow: classify an incoming email, query a knowledge base when it is a product question, call an AWS Lambda function when it is an order question, then draft a reply. They want to build the branching visually and test it in the console rather than writing orchestration code.",
        "Which Amazon Bedrock capability should they use?"
      ],
      "options": [
        "Amazon Bedrock Guardrails",
        "Amazon Bedrock model evaluation",
        "Amazon Bedrock Flows",
        "Amazon Bedrock Provisioned Throughput"
      ],
      "correctAnswers": [
        2
      ],
      "explanation": "Amazon Bedrock Flows provides a visual builder that chains prompts, knowledge bases, agents, AWS Lambda functions and conditional logic into a single workflow that can be tested and versioned from the console, which matches the classify, branch and draft design without orchestration code. Option A filters harmful content and blocks denied topics on the way in and on the way out; it is a safety control and sequences nothing. Option B scores candidate models against a dataset or human reviewers to help choose one, and runs offline before any workflow exists. Option D reserves model capacity for predictable throughput and has no bearing on how the steps are ordered."
    },
    {
      "id": "q61",
      "prompt": [
        "An application calls three different foundation models in Amazon Bedrock depending on the task. Today each call site builds a different request body and unpacks a different response shape, so trying a fourth model means new parsing code. The team also wants multi-turn conversation history expressed the same way for every model.",
        "Which Amazon Bedrock API should the team call?"
      ],
      "options": [
        "The InvokeModel API, passing each model's own native request body",
        "The Converse API, which exposes one consistent request and response format across supported models",
        "The ListFoundationModels API",
        "The CreateModelCustomizationJob API"
      ],
      "correctAnswers": [
        1
      ],
      "explanation": "The Converse API gives Amazon Bedrock a single request and response shape across the models that support it, including a common way to express a multi-turn message list and system instructions, so switching models becomes a configuration change rather than a rewrite. Option A is the lower-level call where each model family expects its own JSON body and returns its own shape, which is precisely the coupling the team wants to remove. Option C returns the catalogue of models available in a Region and performs no inference. Option D starts a customisation job that changes a model's behaviour, which is unrelated to standardising how the application talks to several models."
    },
    {
      "id": "q62",
      "prompt": [
        "A company has two separate needs. Its HR and finance staff want a chat assistant that answers policy and process questions from internal documents held in SharePoint and Amazon S3, respecting each person's existing access rights. Separately, its software engineers want code suggestions inside the IDE and help with questions about AWS services and account resources.",
        "Which services fit the two needs, in that order?"
      ],
      "options": [
        "Amazon Q Developer for the staff assistant, and Amazon Q Business for the engineers",
        "Amazon Q Business for both needs",
        "Amazon Q Developer for both needs",
        "Amazon Q Business for the staff assistant, and Amazon Q Developer for the engineers"
      ],
      "correctAnswers": [
        3
      ],
      "explanation": "Amazon Q Business is the enterprise assistant: it connects to content sources such as SharePoint and Amazon S3, honours the permissions of the person asking, and answers staff questions from that indexed content. Amazon Q Developer is aimed at builders, offering in-IDE code suggestions, code explanation and transformation, and answers about AWS services and the resources in an account. Option A swaps the two, aiming a developer tool at HR staff and a document assistant at engineers. Option B would leave engineers with no IDE integration and no code generation. Option C would give HR and finance a developer product with no connector to their document repositories."
    },
    {
      "id": "q63",
      "prompt": [
        "A regulated insurer is reviewing a proposal to replace the deterministic rules engine that prices simple motor policies with a generative model. The existing engine returns the same premium for the same inputs in under 10 milliseconds, at negligible marginal cost, and every decision can be traced to a published rule.",
        "Which two characteristics of generative AI argue against the replacement?"
      ],
      "options": [
        "The same prompt can produce different outputs on different calls, so identical inputs may not yield an identical premium",
        "Generative models are unable to process numeric values in their input",
        "Generative models must be retrained before they can serve each new request",
        "Generative models cannot be invoked privately from inside an AWS virtual private cloud",
        "Per-request inference latency and token cost are far higher than a deterministic rules lookup"
      ],
      "correctAnswers": [
        0,
        4
      ],
      "explanation": "Two well-documented disadvantages bite here. Generative models sample their output, so the same inputs can produce different wording and therefore a different quoted premium, which is unacceptable for a priced product and hard to defend to a regulator. They also add token cost and inference latency measured in hundreds of milliseconds or more against a lookup that takes under 10 milliseconds at negligible cost. Option B is false: models read numbers in text perfectly well. Option C misstates how inference works, since a trained model serves requests without retraining. Option D is also false, because models can be invoked over private endpoints inside a virtual private cloud."
    },
    {
      "id": "q64",
      "prompt": [
        "A defence contractor must run a text generation model inside an isolated facility that has no internet connectivity of any kind. Before anything is deployed, its legal team must read and approve the exact terms under which the model may be used commercially.",
        "Which two characteristics of open-weight models make them a fit for these requirements?"
      ],
      "options": [
        "Open-weight models are always free of any cost to obtain and to run",
        "Open-weight models always produce higher-quality output than proprietary models",
        "The model weights can be downloaded and hosted on infrastructure the organisation controls",
        "The licence terms are published, so the legal team can read and approve them before any deployment",
        "The complete training dataset is always published alongside the weights"
      ],
      "correctAnswers": [
        2,
        3
      ],
      "explanation": "Open-weight means the trained weights are published for download, so the organisation can host the model on its own hardware inside an air-gapped facility rather than calling a hosted endpoint, and the accompanying licence is published and can be read, assessed against internal policy and approved before use. Those two properties answer the two constraints directly. Option A is false: hosting still costs compute, and some open-weight licences restrict commercial scale or particular uses. Option B is false as a general claim, since quality varies by task and several proprietary models lead common benchmarks. Option E confuses open weights with open data, and training sets are usually not released."
    },
    {
      "id": "q65",
      "prompt": [
        "A team sends the same block of instructions to a model for 14 different report types, substituting only the report name, the audience and the required length. Each of the 14 call sites currently holds its own hand-edited copy of the wording, and three copies have already drifted apart after separate edits.",
        "Which two benefits does moving to a single parameterised prompt template provide?"
      ],
      "options": [
        "It guarantees that the model returns identical output whenever the variables are identical",
        "The shared instruction wording is defined once, so a change applies consistently to all 14 report types",
        "It removes the model's context-window limit for unusually long reports",
        "It eliminates hallucination by constraining what the model is permitted to say",
        "The template can be versioned and tested as one artefact, so a quality regression is easy to trace to a wording change"
      ],
      "correctAnswers": [
        1,
        4
      ],
      "explanation": "A parameterised template holds the shared wording once and substitutes only the variable parts per call, so an edit is made in one place and reaches all 14 report types, which is exactly what stops the drift described. The template also becomes a single versionable artefact that can be reviewed and tested, so a drop in output quality can be traced to a specific wording change rather than hunted across 14 copies. Option A is false, because a sampled model may word its answer differently even with identical variables. Option C is false, since templating changes nothing about the context window. Option D overclaims: consistency is not truthfulness, and a template cannot stop a model asserting something untrue."
    }
  ]
}
