---
/**
 * /<locale>/blog — per-locale post index (one route handles all prefixed
 * locales). English lives at /blog (src/pages/blog/index.astro).
 */
import BlogIndexBody from '../../../components/page/BlogIndexBody.astro';
import { localePaths } from '../../../i18n/paths';
import type { Locale } from '../../../i18n/config';

export const getStaticPaths = localePaths;

const { lang } = Astro.props as { lang: Locale };
---

<BlogIndexBody lang={lang} />
