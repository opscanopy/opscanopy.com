---
/**
 * /es/tools — the OpsCanopy catalog, Spanish.
 * The interactive body (search/chips/sort/empty-state/grid) is the shared
 * ToolsCatalog component — see src/components/ToolsCatalog.astro — so this
 * page owns only the translated header prose and JSON-LD. Tool
 * name/tagline/description/keywords stay English — no translated tool-copy
 * source exists yet.
 */
import Shell from '../../../components/Shell.astro';
import ToolsCatalog from '../../../components/ToolsCatalog.astro';
import { liveTools } from '../../../data/tools';
import { site } from '../../../data/site';
import { DEFAULT_LOCALE, type Locale } from '../../../i18n/config';
import { useTranslations } from '../../../i18n/utils';

const lang: Locale = (Astro.currentLocale as Locale) ?? DEFAULT_LOCALE;
const t = useTranslations(lang);

// ItemList JSON-LD over the live catalog (ordered, with positions).
const itemList = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'OpsCanopy tools',
  description:
    'Herramientas gratuitas y basadas en el navegador para ingenieros de plataforma y DevOps. Sin registro, sin servidores; tus datos permanecen en tu dispositivo.',
  itemListOrder: 'https://schema.org/ItemListOrderAscending',
  numberOfItems: liveTools.length,
  itemListElement: liveTools.map((tool, i) => ({
    '@type': 'ListItem',
    position: i + 1,
    name: tool.name,
    description: tool.tagline,
    url: `${site.url}/${tool.slug}/`,
  })),
};
---

<Shell
  title="Herramientas — OpsCanopy"
  description="Herramientas gratuitas y basadas en el navegador para ingenieros de plataforma y DevOps. Sin registro, sin servidores; tus datos permanecen en tu dispositivo."
  canonical="/tools"
  jsonLd={itemList}
>
  <!-- ── Header block ─────────────────────────────────────────────────── -->
  <section class="border-b border-hairline bg-canvas-soft">
    <div class="container-page py-8 sm:py-10">
      <div class="max-w-3xl">
        <h1 class="display-lg text-balance text-ink">Toda la caja de herramientas, a una pestaña de distancia.</h1>
        <p class="body-md mt-3 max-w-2xl text-pretty text-body">
          Herramientas DevOps especializadas que se ejecutan por completo en tu navegador: gratis, sin registro y nada de lo que pegues sale jamás de tu dispositivo.
        </p>
        <div class="mt-5 flex flex-wrap items-center gap-3">
          <span class="badge">
            <span class="size-1.5 rounded-full bg-success" aria-hidden="true"></span>
            <span class="tabular-nums">{liveTools.length}</span> en activo
          </span>
          <span class="badge">{t('tools.clientSideBadge')}</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ── Live tools ───────────────────────────────────────────────────── -->
  <section aria-labelledby="live-heading" class="bg-canvas">
    <div class="container-page py-16 sm:py-24">
      <ToolsCatalog />
    </div>
  </section>
</Shell>
